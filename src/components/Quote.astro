---
const { body } = Astro.props;
---

<blockquote>{body}</blockquote>

<style>
	blockquote {
		padding-top: var(--block-padded);
		padding-inline: var(--wide-inline);
		position: relative;
	}
</style>
